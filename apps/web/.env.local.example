# Portfolio demo — Vercel frontend only (no Render API / PostgreSQL)
NEXT_PUBLIC_DEMO_MODE=1

# Optional: hide demo panel on login
# NEXT_PUBLIC_HIDE_DEMO_LOGIN=1

# Only used when NEXT_PUBLIC_DEMO_MODE=0 (full stack)
# NEXT_PUBLIC_API_URL=http://localhost:4000
