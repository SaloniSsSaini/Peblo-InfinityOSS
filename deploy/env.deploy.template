# Copy to repo root as .env.deploy — or run:  npm run deploy  (auto-generates)

# Your server IP or domain (no trailing slash)
DEPLOY_HOST=localhost

WEB_PORT=3000
API_PORT=4000

# Built automatically from DEPLOY_HOST — edit if you use HTTPS reverse proxy
PUBLIC_WEB_URL=http://localhost:3000
PUBLIC_API_URL=http://localhost:4000
WEB_ORIGIN=http://localhost:3000

# Auto-generated on first deploy — do not share
POSTGRES_PASSWORD=change-me
JWT_ACCESS_SECRET=change-me
JWT_REFRESH_SECRET=change-me

# Demo login (change in production)
DEMO_EMAIL=demo@peblo.infinityos.app
DEMO_PASSWORD=DemoInfinity2026!
DEMO_NAME=Demo visitor

# Optional — real AI summaries
OPENAI_API_KEY=

# Optional — password reset emails in production
# MAIL_FROM=Peblo InfinityOS <noreply@yourdomain.com>
# RESEND_API_KEY=re_xxx

RUN_DB_SEED=1
